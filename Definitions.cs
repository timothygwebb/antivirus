namespace antivirus
{
    public class Definitions
    {
        public static void LoadDefinitions()
        {
            Logger.LogInfo("Virus definitions loaded", new object[0]);
        }
    }
}
